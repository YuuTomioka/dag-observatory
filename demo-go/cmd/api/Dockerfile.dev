FROM golang:1.24 AS build
WORKDIR /app

COPY go.mod go.sum ./
RUN go mod download
RUN go mod tidy
RUN go mod verify

RUN go install github.com/air-verse/air@v1.61.0

COPY . ./

# Keep the config in-repo under cmd/api/, but run from the module root (/app).
CMD [ "air", "-c", "cmd/api/.air.toml" ]
